# 安全与隐私

## 支持范围

本项目只用于本人拥有或明确获授权维修的美的空调，在一次新的实体主板更换后恢复该空调原有的 22 位机身 SN。当前公开版只认可项目已独立验证的型号/协议组合；其他型号只能做只读分析。

禁止用于身份克隆、批量写入、他人设备、猜测身份、绕过账号/云绑定、规避官方权限或扩展到其他产品类别。

软件锁、追加式事件账本、模板一致性和哈希检查用于减少误操作，但不是硬件 DRM，也不能替代官方维修工具、维修资质或电气安全措施。公开源码可以被修改，因此技术锁不是授权证明。

## 公开仓库数据规则

公开提交只能包含明确标记的合成测试身份。不得提交：

- 完整或部分真实 SN、App 设备编号或可逆派生向量；
- 真实 SSID、BSSID、二维码或设备标签照片；
- 客服凭证、新板证据、家庭/房间信息；
- 运行日志、`TARGET.json`、生成包、ZIP、本地 JSONL 账本或写入标记；
- GitHub 令牌、密码、Cookie、会话或其他凭据；
- 指向使用者本机的绝对路径。

所有 Pull Request 必须运行 `py -3 scripts/public_release_check.py`。测试值必须来自仓库内的合成允许列表；内置客户事件列表必须为空。

如果敏感设备数据已经进入 Git 历史，仅在新提交中删除并不够。必须停止公开发布、移除所有分支/标签/Release 引用、重写历史，并按托管平台要求完成缓存/对象清理后再验证。

## 设备操作规则

- 生成器和验证器只能离线创建/检查文件，不得连接设备。
- 写入前必须核验所有权、可信原机 SN、新实体主板、精确型号和实时服务热点。
- 只读路径不能接受运行时操作码，也不能构造写操作。
- 写入标记必须在打开 TCP 前原子创建并永久保留。
- 写请求最多一条；任何预留、可能发送、超时、异常、ACK 或零响应都禁止同事件重试。
- 完整性通过且可解码的非目标 SN 是永久停止条件。
- 完整性通过但无法解码为 22 位 ASCII 数字的身份载荷也是永久停止条件。
- 零字节、TCP 连接和 ACK 都不是空板、成功或失败证明。

本机锁只在该用户和该电脑上提供尽力保护。将源码或包复制到另一台电脑可能绕过本地历史；这不构成再次写入的授权。

## 报告漏洞

不要在公开 Issue 中粘贴设备证据或凭据。优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口；若该入口不可用，请先创建不含任何设备身份的最小问题说明，等待仓库所有者提供私密渠道。

报告时可包含脱敏后的代码位置、预期行为、实际行为和纯合成复现步骤。不要为了复现而重新运行任何已预留或可能发送过写请求的真实事件。

## 结果解释

结果必须按 `skills/midea-sn-board-restore/references/outcome-classification.md` 分类。`WRITE_RESULT_UNKNOWN` 必须按字面理解：可能已经写入，绝不能通过重跑写模式来“确认”。
